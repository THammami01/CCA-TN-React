// import { CounterEnum, CounterType } from "../types";

// const reducer = (state = 0, action: CounterType): number => {
//   switch (action.type) {
//     case CounterEnum.INCREMENT:
//       return state + 1;
//     case CounterEnum.DECREMENT:
//       return state - 1;
//     default:
//       return state;
//   }
// };

// export default reducer;
